import React from 'react';
import './PageCover.css';

// Represents the page cover that appears behind the new comment popup
// Used to prevent any user interaction while the popup is displayed
export default function PageCover() {
  return <div className="pageCover" />;
}
